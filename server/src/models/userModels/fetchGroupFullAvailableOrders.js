'use strict';

import pool from '../index';
import { errorMessages } from '../../utils/errorMessages';
import { handleErrorModel } from '../utils';
import {
} from './index';


export async function fetchGroupFullAvailableOrders () {
  try {
    //TODO
  } catch (error) {
    return handleErrorModel(error);
  }
}
