'use strict';

const groupCtrl = require('./group');
const itemCtrl = require('./item');
const orderCtrl = require('./order');
const userCtrl = require('./user');
const testsCtrl = require('./tests');

module.exports = {
  groupCtrl,
  itemCtrl,
  orderCtrl,
  userCtrl,
  testsCtrl,
};